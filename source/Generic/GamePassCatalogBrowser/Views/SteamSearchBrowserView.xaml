<UserControl x:Class="SteamSearchBrowser.Views.SteamSearchBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200">
    <Grid>
        <Grid.Resources>
            <Style TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}" />
        </Grid.Resources>
        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="700" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <!-- Left side - Search and Results -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Search Box -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                             FontSize="16" Padding="5"/>
                    <Button Grid.Column="1" Content="Search" Command="{Binding SearchCommand}" 
                            Margin="10,0,0,0" Padding="20,5" FontSize="16"/>
                </Grid>
                
                <!-- Status Message -->
                <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" Margin="0,10,0,0" FontSize="14" Foreground="Gray"/>
                
                <!-- Results List -->
                <ListBox Grid.Row="2" ItemsSource="{Binding SearchResults}" 
                         SelectedItem="{Binding SelectedGame, Mode=TwoWay}"
                         Margin="0,10,0,0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Image Grid.Column="0" Source="{Binding HeaderImage}" 
                                       Width="120" Height="60" Stretch="UniformToFill"/>
                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Bold" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Price}" FontSize="12" Foreground="Green" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
            
            <!-- Right side - Game Details -->
            <ScrollViewer Grid.Column="1" Margin="20,0,0,0" VerticalScrollBarVisibility="Auto">
                <ScrollViewer.Style>
                    <Style TargetType="ScrollViewer">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedGame}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ScrollViewer.Style>
                <StackPanel>
                    
                    <!-- Game Header Image -->
                    <Image Source="{Binding SelectedGame.HeaderImage}" Stretch="Uniform" MaxHeight="200"/>
                    
                    <!-- Game Title -->
                    <TextBlock Text="{Binding SelectedGame.Name}" FontSize="24" FontWeight="Bold" 
                               Margin="0,10,0,0" TextWrapping="Wrap"/>
                    
                    <!-- Price -->
                    <TextBlock Text="{Binding SelectedGame.Price}" FontSize="18" Foreground="Green" 
                               Margin="0,5,0,0"/>
                    
                    <!-- Developers -->
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedGame.Developers.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="Developer: " FontWeight="Bold" Foreground="White"/>
                        <ItemsControl ItemsSource="{Binding SelectedGame.Developers}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" Margin="0,0,5,0" Foreground="White"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                    
                    <!-- Publishers -->
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedGame.Publishers.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="Publisher: " FontWeight="Bold" Foreground="White"/>
                        <ItemsControl ItemsSource="{Binding SelectedGame.Publishers}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" Margin="0,0,5,0" Foreground="White"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                    
                    <!-- Release Date -->
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedGame.ReleaseDate}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="Release Date: " FontWeight="Bold"/>
                        <TextBlock Text="{Binding SelectedGame.ReleaseDate, StringFormat={}{0:MMMM dd, yyyy}}"/>
                    </StackPanel>
                    
                    <!-- Genres -->
                    <StackPanel Margin="0,10,0,0">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedGame.Genres.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="Genres" FontWeight="Bold"/>
                        <ItemsControl ItemsSource="{Binding SelectedGame.Genres}" Margin="0,5,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#444" Padding="8,4" Margin="0,0,5,5" CornerRadius="3">
                                        <TextBlock Text="{Binding}" FontSize="12"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                    
                    <!-- Description -->
                    <TextBlock Text="Description" FontWeight="Bold" Margin="0,10,0,5"/>
                    <TextBlock Text="{Binding SelectedGame.Description}" TextWrapping="Wrap" Margin="0,0,0,10"/>
                    
                    <!-- Buttons -->
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="Add to Library" Command="{Binding AddToLibraryCommand}" 
                                Padding="20,8" FontSize="14" Margin="0,0,10,0"/>
                        <Button Content="View on Steam" Command="{Binding ViewOnSteamCommand}" 
                                Padding="20,8" FontSize="14"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
